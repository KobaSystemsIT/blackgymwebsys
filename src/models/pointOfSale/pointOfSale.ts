export interface POSData {
    saleID: number;
    quantitySold: number;
    totalAmount: number;
    productName: string;
    paymentDescription: string;
}

export interface POSVentas {
    ventas: number;
    paymentDescription: string;
}